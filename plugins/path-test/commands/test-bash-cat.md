---
description: Test path resolution using bash cat with marketplace path
allowed-tools: Bash(bash:*)
---

# Test: Bash Cat with Marketplace Path

Test if bash commands can access files using the full marketplace path (like spec-dev does).

## Instructions

1. Execute: `cat ~/.claude/plugins/marketplaces/personal-configs-plugins/plugins/path-test/reference.txt`
2. Display the output to confirm the path resolved correctly
3. Report success or failure with the path strategy used
